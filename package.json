{
  "name": "nanobundle",
  "version": "0.0.15",
  "type": "module",
  "license": "MIT",
  "bin": "./bin.mjs",
  "repository": {
    "type": "git",
    "url": "https://github.com/cometkim/nanobundle.git"
  },
  "author": {
    "name": "Hyeseong Kim",
    "email": "hey@hyeseong.kim"
  },
  "scripts": {
    "prepack": "yarn build",
    "build": "esbuild src/bin.ts --bundle --minify --format=esm --platform=node --target=node14.8 --outfile=bin.mjs --external:meow --external:esbuild --external:browserslist --external:typescript --external:pretty-bytes"
  },
  "files": [
    "bin.mjs"
  ],
  "engines": {
    "node": ">=14.8.0"
  },
  "dependencies": {
    "browserslist": "^4.0.0",
    "esbuild": "^0.13.0",
    "meow": "^10.1.1",
    "pretty-bytes": "^5.6.0",
    "typescript": "^3.7.0 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^16.7.6"
  }
}
